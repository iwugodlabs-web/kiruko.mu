# Test-only dependencies. Kept OUT of requirements.txt so they are never
# installed into the production image (matches the lean-image approach in the
# Dockerfile / .dockerignore). CI installs this on top of requirements.txt.
pytest>=8.0
httpx>=0.27          # Starlette/FastAPI TestClient transport
